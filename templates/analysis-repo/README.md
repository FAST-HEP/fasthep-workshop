# Analysis Repository Template

This is a lightweight scaffold for future FAST-HEP analysis repositories. It is not a Copier template yet; keep it as a reference layout until package boundaries settle.

The example repository lives under `analysis-name/`.
