[project]
name = "analysis-name"
channels = ["conda-forge"]
platforms = ["linux-64", "osx-64", "osx-arm64"]

[tasks]
run = "fasthep run workflow.yaml --outdir build/analysis-name"
compile = "fasthep compile workflow.yaml --outdir build/analysis-name"
test = "pytest -q"
